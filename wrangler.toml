name = "asp-app"
compatibility_date = "2024-03-04"
pages_build_output_dir = "public"

# Bindings are managed per Pages project (asp-app and asp-app-staging)
# directly in Cloudflare deployment configs to keep environments isolated.

# Local development examples:
#   Production DB: wrangler pages dev public --d1 DB=aspstock-db
#   Staging DB:    wrangler pages dev public --d1 DB=aspstock-staging
